<?xml version = "1.0"?>
<config comment = "{$comment}">
{foreach from = $listServices item = itemService}
	<service id = "{$itemService.identifier}" commandRef = "{$itemService.commandRef}" parent = "{$itemService.parent}" />
{/foreach}
</config>
